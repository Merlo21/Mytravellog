{
  "//": "Il service worker gira in un contesto diverso dall'app: dichiara `lib=webworker` e, standosene dentro tsconfig.app.json, quei tipi finivano nell'INTERO progetto — al punto che window.addEventListener veniva risolto come quello di un worker. Vive qui da solo, e resta comunque controllato: `npm run typecheck` compila anche questo.",
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "WebWorker"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "types": ["vite/client"],
    "skipLibCheck": true,
    "strict": false,
    "noEmit": true,
    "isolatedModules": true,
    "moduleDetection": "force"
  },
  "include": ["src/sw.ts"]
}
